#include <iostream>
#include <list>
#include <vector>



int main() {

    int array[] = {0, 10, 90, 100};

    std::list<int> myList1;
    std::list<int>::iterator it;
    for (it = myList1.begin(); it != myList1.end(); ++it)
        it->a[1] = 5;

    if (!(it->a[0] % 10)) {
        myList1.push_back(myType({7, 7}));
    }
    iteration++;
}


argggggggg


    return 0;
}
